---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.1-0.20231211085026-8e85f220b3ca
  name: backupconfigurations.spaces.upbound.io
spec:
  group: spaces.upbound.io
  names:
    categories:
    - mxe
    kind: BackupConfiguration
    listKind: BackupConfigurationList
    plural: backupconfigurations
    shortNames:
    - bc
    - bcs
    singular: backupconfiguration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupConfiguration defines a Velero configuration for ControlPlanes.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: A BackupConfigurationSpec represents the desired state of
              the Velero configuration for ControlPlanes.
            properties:
              credentials:
                description: Credentials specifies credential information (i.e. secrets)
                  that the Velero storage provider will use. Keep this field empty
                  if authenticating without a secret, e.g. IAM or workload identity.
                properties:
                  secretRef:
                    description: SecretRef contains the reference to a Secret containing
                      provider credentials under the key "cloud".
                    properties:
                      name:
                        description: Name of the secret
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the secret
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  useSecret:
                    description: UseSecret determines if Velero will use the secret
                      defined in the SecretRef field to authenticate to the configured
                      object storage.
                    type: boolean
                required:
                - useSecret
                type: object
              providerPlugin:
                description: ProviderPlugin specifies the object storage provider
                  plugin that Velero will use.
                properties:
                  image:
                    description: Image is the storage provider plugin image to use
                      for Velero as an init container.
                    minLength: 1
                    type: string
                required:
                - image
                type: object
              storageLocation:
                description: StorageLocation specifies a subset of the base spec of
                  the Velero BackupStorageLocation object that will be used for each
                  ControlPlane.
                properties:
                  config:
                    additionalProperties:
                      type: string
                    description: Config specifies provider-specific keys/values to
                      be passed to the Velero object store plugin. See the object
                      storage provider’s plugin documentation for details.
                    type: object
                  objectStorage:
                    description: ObjectStorage specifies the object storage configuration
                      for the given provider.
                    properties:
                      bucket:
                        description: Bucket is the name of the bucket to store backups
                          in.
                        minLength: 1
                        type: string
                    required:
                    - bucket
                    type: object
                  provider:
                    description: Provider is the name of the Velero provider (See
                      the object storage provider’s plugin documentation for the appropriate
                      value to use).
                    minLength: 1
                    type: string
                required:
                - objectStorage
                - provider
                type: object
              userPermissions:
                description: UserPermissions defines what Velero objects users are
                  allowed to perform CRUD operations on. Leave this field empty to
                  disallow user access to all Velero objects.
                items:
                  enum:
                  - Backup
                  - Restore
                  - BackupSchedule
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
