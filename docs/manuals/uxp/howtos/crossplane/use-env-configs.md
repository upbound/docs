---
title: Use EnvironmentConfigs
draft: true
---

This guide explains how to create and use `EnvironmentConfig` objects to share
data between resources in `Composition` pipelines.

## Prerequisites

Before you begin, make sure you have:

* 

## Create an `EnvironmentConfig`

Define the data to share across your Composition pipeline:

```yaml
```

## Select an `EnvironmentConfig` in Compositions

### Select by labels

## Configure selector behavior

### Optional field paths

#### Default values for optional fields

