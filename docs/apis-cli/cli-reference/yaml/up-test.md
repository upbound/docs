Manage and run tests for projects.

#### Usage

```bash
up test [subcommand]
```

#### Subcommands

- `run` - Run project tests.
- `generate` - Generate a Test for a project.

#### Examples

```bash
# Show help
up test --help
```
