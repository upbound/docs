---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: incontrolplaneoverrides.spaces.upbound.io
spec:
  group: spaces.upbound.io
  names:
    categories:
    - spaces
    kind: InControlPlaneOverride
    listKind: InControlPlaneOverrideList
    plural: incontrolplaneoverrides
    singular: incontrolplaneoverride
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InControlPlaneOverride represents resource configuration overrides in
          a ControlPlane. The specified override can be applied on single objects
          as well as claim/XR object hierarchies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              InControlPlaneOverrideSpec defines a configuration override
              on a target object hierarchy in a target ControlPlane with the
              given name.
            properties:
              controlPlaneName:
                description: |-
                  ControlPlaneName is the name of the target ControlPlane where
                  the resource configuration overrides will be applied.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: controlPlaneName is immutable
                  rule: self == oldSelf
              deletionPolicy:
                default: RollBack
                description: |-
                  DeletionPolicy specifies whether when the InControlPlaneOverride object
                  is deleted, the configuration override should be kept (Keep) or
                  rolled back (RollBack).
                enum:
                - RollBack
                - Keep
                type: string
              override:
                description: |-
                  Override denotes the configuration override to be applied on the target
                  object hierarchy. The fully specified intent is obtained by serializing
                  the Override.
                properties:
                  metadata:
                    description: Metadata specifies the patch metadata.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations represents the Kube object annotations.
                          Only the following annotations are allowed to be patched:
                          - crossplane.io/paused
                          - spaces.upbound.io/force-reconcile-at
                        type: object
                        x-kubernetes-validations:
                        - message: Only the crossplane.io/paused and spaces.upbound.io/force-reconcile-at
                            annotations are allowed
                          rule: self.all(k, k == 'crossplane.io/paused' || k == 'spaces.upbound.io/force-reconcile-at')
                    type: object
                type: object
              propagationPolicy:
                default: None
                description: |-
                  PropagationPolicy specifies whether the configuration override will be
                  applied only to the object referenced in TargetRef (None), after an
                  ascending or descending hierarchy traversal will be done starting with
                  the target object.
                enum:
                - None
                - Ascending
                - Descending
                type: string
              targetRef:
                description: |-
                  TargetRef is the object reference to a Kubernetes API object where the
                  configuration override will start. The controller will traverse the
                  target object's hierarchy depending on the PropagationPolicy. If
                  PropagationPolicy is None, then only the target object will be updated.
                properties:
                  apiVersion:
                    description: APIVersion of the referenced object.
                    minLength: 1
                    type: string
                  kind:
                    description: Kind of the referenced object.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the referenced object.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - controlPlaneName
            - override
            - targetRef
            type: object
          status:
            description: |-
              InControlPlaneOverrideStatus defines the status of an InControlPlaneOverride
              object.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              objectRefs:
                items:
                  description: |-
                    PatchedObjectStatus represents the state of an applied patch to an object
                    in the target hierarchy.
                  properties:
                    apiVersion:
                      description: APIVersion of the referenced object.
                      minLength: 1
                      type: string
                    kind:
                      description: Kind of the referenced object.
                      minLength: 1
                      type: string
                    message:
                      description: Message holds an optional detail message detailing
                        the observed state.
                      type: string
                    name:
                      description: Name of the referenced object.
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the referenced object.
                      type: string
                    reason:
                      description: Reason is the reason for the target objects override
                        Status.
                      type: string
                    status:
                      description: Status of the configuration override.
                      enum:
                      - Success
                      - Skipped
                      - Error
                      type: string
                    uid:
                      description: Metadata UID of the patch target object.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - reason
                  - status
                  type: object
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
